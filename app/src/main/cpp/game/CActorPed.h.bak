#pragma once
#include "entity.h"   // Assuming CEntity is declared here
#include "types.h"    // VECTOR, PED_TYPE, etc.

class CActorPed : public CEntity
{
public:
    CActorPed(uint16_t usModel, const VECTOR& vecPosition, float fRotation, float fHealth, bool bInvulnerable);
    virtual ~CActorPed();

    void Destroy();
    void SetHealth(float fHealth);
    void SetDead();
    void ForceTargetRotation(float fRotation);
    void ApplyAnimation(const char* szAnimName, const char* szAnimFile, float fDelta, int bLoop, int bLockX, int bLockY, int bFreeze, int uiTime);

    PED_TYPE* m_pPed = nullptr;   // Initialize pointer to nullptr
};
